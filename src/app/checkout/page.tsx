// ROUTE STUB — owned by task 05 (checkout).
// It exists so five parallel branches do not each create this file. Task 05 (checkout)
// replaces its contents entirely.
// Authenticated route: src/proxy.ts sends signed-out visitors to /login.
export default function Page() {
  return <h1>Checkout</h1>
}
